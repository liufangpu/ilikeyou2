<?xml version="1.0" encoding="UTF-8" ?>    
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">  
<mapper namespace="site.jjilikeyou.www.dao.DataMapper"> 
	<select id="sendMsg">
		insert into message(email,fullname,message,ip,ipName)values(#{email},#{fullname},#{message},#{ip},#{ipName})
	</select>
</mapper>